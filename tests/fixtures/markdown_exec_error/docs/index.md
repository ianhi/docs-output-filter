# Test Page

This page has an intentional error in a code block.

```python exec="on" session="test"
x = 1
y = 2
print(f"x + y = {x + y}")
raise ValueError("INTENTIONAL TEST ERROR")
```

Some text after the code block.
